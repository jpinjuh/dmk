// React
import React from "react";

// MUI
import Typography from "@material-ui/core/Typography";

const Admin = () => {
  return <Typography variant="h4">Admin panel (private route)</Typography>;
};

export default Admin;
