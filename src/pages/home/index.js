// React
import React from "react";

// MUI
import Typography from "@material-ui/core/Typography";

const Home = () => {
  return <Typography variant="h4">Homescreen (public route)</Typography>;
};

export default Home;
