// React
import React from "react";

// MUI
import Typography from "@material-ui/core/Typography";

const Posts = () => {
  return <Typography variant="h4">Posts (public route)</Typography>;
};

export default Posts;
