// MUI
import { createMuiTheme } from "@material-ui/core/styles";

const theme = createMuiTheme({
  palette: {
    type: "dark",
    primary: {
      main: "#283f97"
    },
    secondary: {
      main: "#ec1b78"
    },
    spacing: 8
  }
});

export default theme;
